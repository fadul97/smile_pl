#include <stdio.h>

int main(int argc, char* argv[]){
    int var = 10;
    var = a + 10;
    char* _var;
    if (var <= _var )
    {
    }
    else if (_var >= a )
    {
    }
    else
    {
    }
    printf("oi");
    printf("    for (int a = 0; a <= 100; a++) {
    printf("_var");
    scanf("UNKNOWN", &var);
    for (int a = 0; a <= 100; a++) {
    }
    for (int b = 10; b >= 0; b--) {
    }
    while (a && 10 || !b && c )
    {
    }
    return 0;
}