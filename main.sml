TheBeginning
    var <=> 10 is int..
    a <=> var + 10 is int..
    _var <=> "Hello, World!" is string..

    if var < a then (:
    :) elif var > a then (:
    :) ifnot (:
    :)

    write("oi")

    write({var})
    write({a})
    write({_var})

    read(var)
    read(_var)

    for 0...100 go (:
    :)

    for 10...0 go (:
    :)

    while a && 10 || !b! && c (:
    :)
TheEnd