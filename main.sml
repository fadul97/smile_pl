TheBeginning
    var <=> 10 is int..
    a <=> var + 10 is int..
    _var <=> "Hello, World!\n" is string..

    if var < a then (:
    :) elif var > a then (:
    :) ifnot (:
    :)

    write("\n")
    write("oi\n")

    write({var})
    write("\n")
    write({a})
    write("\n")
    write({_var})

    read(var)
    read(_var)

    for 0...100 go (:
    :)

    for 10...0 go (:
    :)

    while a (:
    :)
TheEnd