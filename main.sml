TheBeginning
    var <=> 10 is int..
    var1 <=> "Hello, World!"..
    _var <=> 0.5..

    if var <= _var then (:
    :) elif _var >= a then (:
    :) ifnot (:
    :)

    write("oi")

    for 0...100 go (:
    :)

    for 10...0 go (:
    :)

    while a && 10 || !b! && c (:
    :)
TheEnd