TheBeginning
    var <=> 10 is int..
    x <=> var + 10 is int..
    _var <=> "Hello, World!\n" is string..

    if var < x then (:
        b <=> 5 is int..
        write("Valor para b: ")
        read(b)
        write({_var})
    :) elif var > x then (:
    :) ifnot (:
    :)

    write("\n")
    write("oi\n")

    write({var})
    write("\nx = ")
    write({x})
    write("\n")
    write({_var})

    for 0...100 go (:
    :)

    for 10...0 go (:
    :)

    while x > 10(:
        write({_var})
        x <=> x - 5..
    :)

    write("\nx = ")
    write({x})
    write("\n")
    write("Valor para var: ")
    read({var})
    write("\n")

TheEnd