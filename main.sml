TheBeginning
    var <=> 10 is int..
    var <=> a + 10..
    _var is string..

    if var <= _var then (:
    :) elif _var >= a then (:
    :) ifnot (:
    :)  

    write("oi")
    
    write("oi {   _var    }")

    read(var)

    for 0...100 go (:
    :)

    for 10...0 go (:
    :)

    while a && 10 || !b! && c (:
    :)
TheEnd